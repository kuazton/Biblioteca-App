@model CRUD.Models.Editorial
@{
    ViewData["Title"] = "Eliminar editorial";
    var crudHeader = new CrudPageHeaderViewModel
    {
        Title = "Eliminar editorial",
        IconClass = "fa-trash-can",
        TitleDanger = true,
        Subtitle = "¿Confirma que desea eliminar esta editorial?"
    };
}

<div class="container-fluid">
    <partial name="_CrudPageHeader" model="crudHeader" />
    <div class="row justify-content-center">
        <div class="crud-form-shell">
            <div class="card shadow-sm crud-card border-danger">
                <div class="card-body">
                    <div class="alert alert-warning" role="alert">
                        <i class="fa-solid fa-triangle-exclamation me-2"></i>
                        <strong>Atención.</strong> Esta acción no se puede deshacer.
                    </div>
                    <dl class="row crud-dl mb-0">
                        <dt class="col-sm-4">@Html.DisplayNameFor(m => m.Nombre)</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(m => m.Nombre)</dd>
                    </dl>
                    <form asp-action="Delete" class="mt-4">
                        <input type="hidden" asp-for="Id" />
                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a asp-action="Index" class="btn btn-secondary me-md-2">
                                <i class="fas fa-times"></i> Cancelar
                            </a>
                            <button type="submit" class="btn btn-danger">
                                <i class="fas fa-trash-can"></i> Eliminar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
